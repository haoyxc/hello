<form method="POST"
    style="max-width: 300px; margin: auto; margin-top: 50px; border: 0.5px solid gray; padding: 30px 30px 30px 30px">
    <h3>{{#if edit}} Edit {{else}}Create{{/if}} Contact</h3>
    <div class="form-group">
        <label>Name</label>
        <input type="text" name="name" class="form-control"value={{contact.name}}>
    </div>
    <div class="form-group">
        <label>Phone Number</label>
        <input type="text" name="phone" class="form-control"value={{contact.phone}}>
    </div>
    <div class="form-group">
        <button class="btn btn-info">{{#if edit}} Edit {{else}}Create{{/if}}</button>
    </div>
</form>